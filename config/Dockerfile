# Используем образ Python 3.9
FROM python:3.9-slim

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл с зависимостями (на случай если в будущем они появятся)
COPY requirements.txt .

# Устанавливаем зависимости (если будут указаны)
RUN pip install --no-cache-dir -r requirements.txt || true

# Копируем конфигурационный файл в контейнер
COPY . .

# Контейнер будет содержать только конфигурационные файлы, дополнительных команд не требуется.
